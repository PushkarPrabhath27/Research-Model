# ==============================================================================
# GriceBench — Gricean Maxim Dialogue Systems Research
# .gitignore
# ==============================================================================
#
# This file specifies intentionally untracked files that Git should ignore.
# The project contains trained neural network checkpoints (DeBERTa, T5, GPT-2),
# large NLP datasets, FAISS indices, and a Python virtual environment — none
# of which belong in version control.
#
# Model weights are hosted externally and can be downloaded via:
#   python GriceBench/scripts/download_models.py --all
#
# For dataset reproduction, see GriceBench/scripts/download_data.py.
# ==============================================================================


# ──────────────────────────────────────────────────────────────────────────────
# 1. PYTHON BUILD & RUNTIME ARTIFACTS
# ──────────────────────────────────────────────────────────────────────────────
# Byte-compiled files, C extensions, and packaging artifacts generated by
# setuptools, pip, and the CPython interpreter.

__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.eggs/
*.whl

# Distribution & build directories
build/
dist/
sdist/
develop-eggs/
downloads/
eggs/
lib/
lib64/
parts/
var/
wheels/
.installed.cfg


# ──────────────────────────────────────────────────────────────────────────────
# 2. VIRTUAL ENVIRONMENTS
# ──────────────────────────────────────────────────────────────────────────────
# The project uses a local venv (grice_env/) for dependency isolation.
# These should never be committed — use `pip install -r requirements.txt`
# to reproduce the environment.

grice_env/
venv/
.venv/
env/
ENV/
.env/


# ──────────────────────────────────────────────────────────────────────────────
# 3. TRAINED MODEL WEIGHTS & CHECKPOINTS
# ──────────────────────────────────────────────────────────────────────────────
# GriceBench trains three neural models:
#   • Detector  — DeBERTa-v3-base  (~735 MB .pt checkpoint)
#   • Repair    — T5-base          (~900 MB .safetensors)
#   • Generator — GPT-2-medium+DPO (~1.4 GB adapter weights)
#
# These are too large for GitHub (100 MB limit) and must be downloaded
# separately or hosted on Hugging Face Hub / Google Drive.

# PyTorch & SafeTensors model files
*.pt
*.pth
*.safetensors
*.ckpt

# Hugging Face / TRL binary artifacts
*.bin

# ONNX exports (from scripts/export_onnx.py)
*.onnx

# Explicit checkpoint paths (for clarity)
GriceBench/best_model_v2.pt
GriceBench/models/detector/best_model.pt
GriceBench/models/repair/repair_model/model.safetensors
GriceBench/models/dpo/


# ──────────────────────────────────────────────────────────────────────────────
# 4. LARGE DATASETS & PROCESSED DATA
# ──────────────────────────────────────────────────────────────────────────────
# Raw dialogue corpora (TopicalChat, Wizard-of-Wikipedia, FaithDial, LIGHT)
# and processed training data can be 50–160 MB per file. These are excluded
# and can be regenerated via the data pipeline scripts.

# Raw corpus sub-directories
GriceBench/data_raw/wizard_of_wikipedia/dialogs/
GriceBench/data_raw/topicalchat/conversations/
GriceBench/data_raw/light/dialogues/

# Compressed dataset archives
*.json.gz
*.tar.gz
*.zip

# Large JSON data files exceeding GitHub limits (50+ MB each).
# These are regenerated by the data pipeline (scripts/download_data.py,
# scripts/prepare_detector_data.py, scripts/prepare_repair_data.py).
GriceBench/data_processed/train_examples.json
GriceBench/data_processed/val_examples.json
GriceBench/data_processed/test_examples.json
GriceBench/data_raw/topical_chat/train.json

# FAISS similarity search indices (from scripts/build_retrieval_system.py)
*.faiss
*.pkl


# ──────────────────────────────────────────────────────────────────────────────
# 5. EXPERIMENT RESULTS & OUTPUT DIRECTORIES
# ──────────────────────────────────────────────────────────────────────────────
# Kaggle run outputs and intermediate experiment artifacts. These are
# ephemeral and should be re-generated from notebooks/scripts.

GriceBench/results/*/
GriceBench/*_output/
GriceBench/dpo-scoring-and-filtering_output/


# ──────────────────────────────────────────────────────────────────────────────
# 6. JUPYTER NOTEBOOK ARTIFACTS
# ──────────────────────────────────────────────────────────────────────────────
# Checkpoint directories auto-created by Jupyter/JupyterLab.
# Notebook .ipynb files themselves ARE tracked.

.ipynb_checkpoints/
*.ipynb_checkpoints/
.virtual_documents/


# ──────────────────────────────────────────────────────────────────────────────
# 7. IDE & EDITOR CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────────
# Developer-specific settings that vary per machine.

.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.code-workspace


# ──────────────────────────────────────────────────────────────────────────────
# 8. OPERATING SYSTEM FILES
# ──────────────────────────────────────────────────────────────────────────────

.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/


# ──────────────────────────────────────────────────────────────────────────────
# 9. LOGS, EXPERIMENT TRACKING & TEMPORARY FILES
# ──────────────────────────────────────────────────────────────────────────────

*.log
logs/
wandb/
mlruns/
*.tmp
*.bak
*.swp
*~


# ──────────────────────────────────────────────────────────────────────────────
# 10. LOCAL TOOLING & AGENT WORKSPACE
# ──────────────────────────────────────────────────────────────────────────────
# The .agent/ directory contains local AI-assistant configuration files
# (skills, workflows) that are specific to the development environment.

.agent/
.gemini/


# ──────────────────────────────────────────────────────────────────────────────
# 11. DIRECTORY STRUCTURE MARKERS
# ──────────────────────────────────────────────────────────────────────────────
# .gitkeep files are used to preserve empty directory structures in Git.
# This rule explicitly un-ignores them so they are always tracked.

!.gitkeep
